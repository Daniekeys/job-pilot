# Progress Tracker

Update this file after every completed feature. Any AI agent reading this should immediately know what is done, what is in progress, and what is next.

---

## Current Status

**Phase:** Phase 5 — Dashboard
**Last completed:** 17 Analytics Charts — PostHog Data
**Next:** None — all 17 build-plan features complete

---

## Progress

### Phase 1 — Foundation

- [x] 01 Homepage
- [x] 02 Auth
- [x] 03 PostHog Initialization
- [x] 04 Database Schema

### Phase 2 — Profile Page

- [x] 05 Profile Page — Full UI
- [x] 06 Profile Save Logic
- [x] 07 AI Profile Extraction from Resume
- [x] 08 Resume PDF Generation from Profile

### Phase 3 — Find Jobs Page

- [x] 09 Find Jobs Page — Full UI
- [x] 10 Adzuna Job Discovery
- [x] 11 Filter + Sort + Pagination

### Phase 4 — Job Details Page

- [x] 12 Job Details Page — Full UI
- [x] 13 Company Research Agent

### Phase 5 — Dashboard

- [x] 14 Dashboard Page — Full UI
- [x] 15 Stats Bar — Real Data
- [x] 16 Recent Activity — Real Data
- [x] 17 Analytics Charts — PostHog Data

---

## Decisions Made During Build

- **01 Homepage:** Built directly from `context/designs/landing-page.png` rather than the section list in build-plan.md — design took precedence per ui-rules.md ("Design assets are available — use them as the source of truth"). No `HowItWorks.tsx` — the design has two alternating feature/image blocks instead of numbered steps.
- **01 Homepage:** The four screenshot-style assets in `public/images/` (`dashboard-demo.png`, `jobs-lists.png`, `agnet-log.png`, `user-icon.png`) are embedded directly as `<Image>` — they already match the mockup pixel-for-pixel, so no UI was rebuilt with HTML/CSS for those sections.
- **01 Homepage:** "Get Started" / "Start for free" / "Find Your First Match" all link to `/login` for now. Auth doesn't exist yet (Phase 1 → 02), so the conditional "→ /dashboard if authenticated" behavior from build-plan.md isn't wired up yet — revisit once auth lands.
- **01 Homepage:** Primary CTA buttons use `bg-overlay` (dark), not `bg-accent` (purple) — matches the actual button color in the design screenshot. The purple `bg-accent` primary button style from ui-tokens.md still applies to in-app primary actions (e.g. Find Jobs, Save Profile).
- **01 Homepage:** `public/logo.png` is a combined icon+wordmark PNG, rendered as one `<Image>` in Navbar/Footer rather than rebuilding the icon with the CSS gradient spec in ui-tokens.md (that spec's `#4A2EC5` stop isn't an actual token, so using the provided asset avoided introducing a hardcoded hex).
- **02 Auth:** The project already had `@insforge/sdk` (not the `@insforge/ssr` package architecture.md's code samples reference) with a real `@insforge/sdk/ssr` submodule — `createBrowserClient`, `createServerClient`, `createAuthActions`, `updateSession` — confirmed by reading the installed package's type defs and compiled source. This submodule is what `lib/insforge-client.ts` / `lib/insforge-server.ts` already used from 01 Homepage, so all new auth code follows it instead of the generic `createClient()` pattern in InsForge's MCP docs.
- **02 Auth:** Next.js 16 renamed `middleware.ts` → `proxy.ts` (exported function is `proxy`, not `middleware`) — confirmed in `node_modules/next/dist/docs`. Route protection lives in `proxy.ts` at the project root, not `middleware.ts`.
- **02 Auth:** OAuth callback is `app/(auth)/callback/route.ts` (a Route Handler), not `page.tsx` as architecture.md's folder list shows — cookies can only be written in a Server Action or Route Handler, never during Server Component render, and the callback step must persist the exchanged session cookies before redirecting to `/dashboard`.
- **02 Auth:** `signInWithOAuth()`'s PKCE `codeVerifier` is persisted in a short-lived httpOnly cookie (`oauth_code_verifier`, 10 min, name in `lib/utils.ts`) between the sign-in redirect and the callback exchange — the SDK's own `codeVerifier` persistence uses browser `sessionStorage`, which doesn't exist when `signInWithOAuth` is called from a Server Action.
- **02 Auth:** `app/page.tsx` (homepage) now redirects authenticated users straight to `/dashboard` server-side, resolving the "revisit once auth lands" note left in 01 Homepage. CTA button hrefs stay `/login` since they only render for logged-out users now.
- **02 Auth:** No design mockup exists for `/login` (only dashboard/find-jobs/job-details/landing-page in `context/designs/`) — built from ui-rules.md/ui-tokens.md as a centered card with two OAuth buttons. Brand icons (Google, GitHub) are inline SVGs since the installed `lucide-react` version ships no brand/logo icons.
- **02 Auth — manual step still needed:** InsForge backend metadata shows `allowedRedirectUrls: []`. There is no MCP tool to set this — add `http://localhost:3000/callback` (and the production URL later) in the InsForge dashboard's Auth settings before testing sign-in end-to-end.
- **02 Auth:** Post-login redirects to `/dashboard`, which doesn't exist until Phase 5 — this will 404 until then, expected per the build order.
- **02 Auth — documented exception:** `GoogleIcon` in `app/(auth)/login/page.tsx` hardcodes Google's 4 official brand hex colors (`#4285F4`, `#34A853`, `#FBBC05`, `#EA4335`) inline, which a code review flagged against ui-tokens.md's "never hardcode hex" rule. These are externally mandated brand colors on a fixed multi-color logo, not theme colors — same category as `--color-linkedin` in ui-tokens.md. Follow-up: add `--color-google-blue/green/yellow/red` tokens to `app/globals.css`'s `@theme` block and reference them via `var(--color-google-*)` instead of inline hex, matching the LinkedIn precedent exactly.
- **02 Auth — known issue from code review:** `proxy.ts` calls `updateSession()` against a `NextResponse.next()` response, but the two redirect branches return a *different* `NextResponse.redirect(...)` object — any cookie refresh/clear queued by `updateSession()` is silently dropped on those paths. Not yet fixed. Fix: copy `response.cookies.getAll()` onto the redirect response (or build the redirect response first and pass its cookie store into `updateSession()`).
- **02 Auth — resolved:** InsForge backend's `allowedRedirectUrls` is no longer empty — `get-backend-metadata` now shows `http://localhost:3000/callback` and `http://localhost:3001/callback` configured. The "manual step still needed" note from earlier sessions is done.
- **04 Database Schema:** All four tables (`profiles`, `agent_runs`, `jobs`, `agent_logs`) created via `run-raw-sql` with exact columns from architecture.md, plus the `resumes` storage bucket (private). Decisions, confirmed with the developer via `/architect`:
  - **`profiles.id`** is the auth user's id itself (`PRIMARY KEY id uuid REFERENCES auth.users(id) ON DELETE CASCADE`) — no separate `user_id` column on this table, matching the 1:1 design in architecture.md.
  - **Profile row auto-creation:** a `SECURITY DEFINER` trigger function `handle_new_user()` fires `AFTER INSERT ON auth.users` and inserts an empty `profiles` row (`is_complete = false`, `email` pre-filled). Verified live on `auth.users` via `pg_trigger`. Every authenticated user has a profile row from the moment they sign up — no "no profile yet" state to handle anywhere downstream.
  - **CHECK constraints** added on every enum-like column instead of plain `text`: `profiles.experience_level/remote_preference/cover_letter_tone/work_authorization`, `jobs.source/job_type`, `agent_runs.status`, `agent_logs.level`. Enforces the invariants architecture.md already states in prose, at the DB layer.
  - **`resumes` bucket created with `isPublic: false`** — resume bytes only retrievable via authenticated `insforge.storage.download()`, matching architecture.md's "authenticated users only, own files only" literally (a public bucket would have allowed anyone with the URL to fetch the PDF without auth).
  - **RLS policies** (all using `auth.uid()`, verified via `get-table-schema` on every table): `profiles` has SELECT + UPDATE only (no INSERT/DELETE — the trigger is the only writer of new rows, and nothing in scope ever deletes a profile). `agent_runs` and `jobs` have SELECT + INSERT + UPDATE (UPDATE needed — agent writes go back and update `agent_runs.status/jobs_found/completed_at` after a run finishes, and `jobs.company_research` after research). `agent_logs` has SELECT + INSERT only (logs are never updated or deleted).
  - **`updated_at` auto-trigger** (`set_updated_at()`) added on `profiles` only — the only table with an `updated_at` column.
  - All FKs use `ON DELETE CASCADE` down the chain (`auth.users` → `profiles` → `agent_runs`/`jobs` → `agent_logs`) — nothing in scope deletes users, but this keeps the DB consistent if it ever happens.
- **05 Profile Page — Full UI:** Built `app/profile/page.tsx` + `components/profile/{CompletionIndicator,ResumeUpload,ProfileForm}.tsx` directly from `context/designs/profile.png`, with mock data only — no Server Action, no InsForge calls, "Save Profile" and "Generate Resume from Profile" buttons are currently inert. Added `types/index.ts` (`Profile`, `WorkExperience`, `Education`, etc.) matching architecture.md's `profiles` table shape, for 06 to reuse.
  - **New `AppNavbar` component** (`components/layout/AppNavbar.tsx`) — the existing `Navbar` in the registry is the logged-out marketing variant (logo + links + "Start for free" button); `/profile` needed the logged-in variant from ui-rules.md (three links, active-color highlighting, no CTA button). Used directly in `app/profile/page.tsx`, will be reused by `/dashboard` and `/find-jobs` once those are built for real.
  - **Profile completion ring + missing-field pills use `warning` (orange) tokens** — ui-tokens.md doesn't define a specific color for profile-completion state (only for match-score bars, a different concept), so orange was chosen as the closest semantic "needs attention" color and confirmed against the design screenshot.
  - **`cover_letter_tone` omitted from the form** — present in architecture.md's `profiles` schema and build-plan.md's Job Preferences field list, but not visible in `context/designs/profile.png` (Job Preferences ends at Preferred Locations). Design took precedence per ui-rules.md. Flagged for the developer to confirm before 06 — see `ui-registry.md`'s `ProfileForm` entry.
  - Visually verified against `context/designs/profile.png` via real screenshots from the developer's authenticated browser session (banner/ring/badges, resume card, and full form all confirmed matching).
- **Added temporary placeholder pages** `app/dashboard/page.tsx` and `app/find-jobs/page.tsx` (using a new shared `components/layout/ComingSoon.tsx`) — not part of feature 05, requested separately by the developer so `AppNavbar`'s Dashboard/Find Jobs links don't 404 while testing. **These are not the real Phase 3/5 UI builds** — `ComingSoon` and its two usages should be deleted once features 09 (Find Jobs — Full UI) and 14 (Dashboard — Full UI) are actually built.
- **06 Profile Save Logic:** Planned via `/architect` before building. Key decisions:
  - **Completion percentage / missing fields are computed on every page load**, never persisted — `lib/utils.ts`'s `getProfileCompletion()` derives both from the live profile row against a fixed 12-item required-field list (Full Name, Phone, Location, Current Title, Experience Level, Years of Experience, Skills, Work Experience, Education, Job Titles Seeking, Remote Preference, Work Authorization). Only `is_complete` (boolean) is written to the DB — matches the actual `profiles` schema, which has no `completion_percentage`/`missing_fields` columns.
  - **Resume upload is fully decoupled from the profile field save.** `ResumeUpload`'s file input calls `uploadResume()` immediately on selection/drop; `ProfileForm`'s "Save Profile" button calls `saveProfile()` independently. Avoided lifting shared state into a new client wrapper across the two sibling components.
  - **`job_titles_seeking` and `preferred_locations`** stay single comma-separated text inputs in the UI (no change from 05) — `saveProfile` splits them into `text[]` on write, `app/profile/page.tsx` joins the array back to a comma string on read.
  - **`cover_letter_tone` stays out of scope** — no dropdown added, column stays `null`. Same "design takes precedence" precedent as 05.
  - **`profile_completed` PostHog event fires only on the `is_complete` false → true transition** — `saveProfile` reads the existing value before updating and compares, so re-saving an already-complete profile never re-fires it.
  - **SDK deviations from `library-docs.md` found and followed instead of the doc** (doc is outdated against the installed `@insforge/sdk`): current-user lookup is `insforge.auth.getCurrentUser()`, not `auth.getUser()`; DB calls are `insforge.database.from(...)`, not `insforge.from(...)`; `storage.from(bucket).upload(path, file)` takes no `contentType`/`upsert` options and returns `{ data: { url, key, size, mimeType, uploadedAt } }` directly — `resume_pdf_url` is saved straight from that `url` field. Upsert behavior is implemented manually (`bucket.remove(path)` before `bucket.upload(path, file)`) since the SDK has no `upsert` flag.
  - **PostHog server calls use the actual installed `lib/posthog-server.ts` singleton (`getPostHogServer()`)**, not the create-new-instance-and-`shutdown()`-per-call pattern shown in `library-docs.md`/`code-standards.md` — the installed file caches one client at module scope, and `flushAt: 1`/`flushInterval: 0` already send each event immediately, so calling `shutdown()` here would have broken the singleton for every later caller (Phase 3/4's `job_found`/`company_researched` events). `library-docs.md` should be revisited if this pattern starts causing missed events on process exit.
- **06 Profile Save Logic — fixed after `/review`: "View current resume" 401'd (`AUTH_INVALID_CREDENTIALS`).** Root cause: `uploadResume()` saved `storage.upload()`'s returned `url` directly into `resume_pdf_url`, but that field is only browser-fetchable for **public** buckets — `resumes` is private (04's deliberate choice), and a plain `<a href>` navigation sends no bearer token. Fixed: `resume_pdf_url` now stores the **storage path** (`{user_id}/resume.pdf`), and `app/profile/page.tsx` mints a fresh `storage.from("resumes").createSignedUrl(path, 86400)` (24h TTL) on every render to pass to `ResumeUpload` for display; `uploadResume()` does the same right after upload so the link works immediately without a reload. The one existing real row with the old broken URL was corrected to the path format (developer-approved direct SQL update) — no other rows affected. **Any future code that reads `profiles.resume_pdf_url` directly must treat it as a path, not a usable URL** — always sign it through `createSignedUrl()` before handing it to a browser.
- **07 AI Profile Extraction from Resume:** Planned via `/architect`. Key decisions:
  - **Deliberate deviation from architecture.md/code-standards.md's "always `gpt-4o`" rule** — the developer explicitly requested a 3-provider fallback rather than a single hardcoded model, provisioned via three separate keys already in `.env.local` (`GEMINI_API_KEY`, `GROQ_API_KEY`, `OPENAI_API_KEY`). **Important naming catch:** the developer initially said "Grok" (xAI) but the actual key in `.env.local` is `GROQ_API_KEY` — confirmed with the developer this means Groq (the fast-inference host for open models), not xAI's Grok, before building anything. **Provider order is OpenAI → Gemini → Groq** (`lib/ai-client.ts`'s `PROVIDERS` array) — initially built Gemini → Groq → OpenAI, then reordered same-session once live testing confirmed the developer's OpenAI key is paid/active while Groq's key was invalid and a prior OpenAI key had been over quota; OpenAI now goes first since it's the confirmed-working key, with Gemini/Groq as fallback.
  - **One shared client for all three providers** — `lib/ai-client.ts`'s `callWithFallback()`. Gemini and Groq both expose OpenAI-compatible chat-completions endpoints (`https://generativelanguage.googleapis.com/v1beta/openai/` and `https://api.groq.com/openai/v1`), so the already-approved `openai` npm package is reused for all three (just swapping `baseURL`/`apiKey`/`model`) instead of adding `@google/generative-ai` or `groq-sdk` as new dependencies. Built as shared infra (not feature-07-only) so 08/10/13 can adopt the same fallback later.
  - **Per-provider timeout 20s, `maxRetries: 0`** on each OpenAI client instance — moves to the next provider immediately on any thrown error, timeout, or a response that fails the Zod schema check, rather than retrying the same provider or relying on the SDK's internal retry.
  - **JSON parsing is provider-agnostic by design** — no provider is asked for `response_format: json_object`/`json_schema` (Gemini's OpenAI-compat layer support for this was unconfirmed at build time); instead every provider gets the same "return ONLY valid JSON" instruction, response content is fence-stripped and `JSON.parse`'d, then validated with a Zod schema (`agent/extractor.ts`) before being accepted. This avoids needing different request shapes per provider.
  - **Critical model-currency finding, surfaced to the developer before building:** live web research during this session found that **GPT-4o's OpenAI API access was retired February 17, 2026** and Groq deprecated `llama-3.1-8b-instant`/`llama-3.3-70b-versatile` on June 17, 2026 (9 days before this session) — both of which architecture.md/code-standards.md still name explicitly. Current models used instead: `gemini-3.5-flash` (Gemini), `openai/gpt-oss-20b` (Groq — their own recommended migration target), `gpt-5.4-mini` (OpenAI). **architecture.md and code-standards.md still say "gpt-4o" project-wide and have not been updated** — any future feature (08 Resume Generation, 10 Job Matching, 13 Company Research) reading those docs literally will reference a dead model. Flagging for a documentation pass; not fixed in this session since it was out of scope for feature 07's diff.
  - **`pdf-parse` is now v2** (installed `2.4.5`) — `library-docs.md`'s documented pattern (`import pdf from "pdf-parse"; await pdf(buffer)`) is the old v1 API and no longer compiles. Actual usage: `new PDFParse({ data: buffer }).getText()` returns `{ text, pages, total }`, then `await parser.destroy()`. `library-docs.md` should be updated before any other feature touches PDF parsing.
  - **State lifted out of `ProfileForm` into a new `ProfileEditor` wrapper** (see `ui-registry.md`) — revises 06's "fully decoupled siblings" decision, which is no longer tenable now that AI-extracted data must land inside `ProfileForm`'s fields for the user to review before saving.
  - **Extraction overwrites every extractable field on every click** (developer's explicit choice over "only fill empty fields") — resume is treated as the source of truth when the button is clicked; user reviews/edits after, same as build-plan.md's stated flow. Non-extractable fields (`id`, `email`, `resumePdfUrl`, `isComplete`) are never touched since they're not part of the extraction schema.
  - **Extraction failures use the API Route Handler error pattern** (`console.error("[api/resume/extract]", ...)`, generic message), not `agent_logs` — there's no `agent_run` row for this flow to attach a log entry to, unlike the Find Jobs/Company Research agents which are always tied to a run.
  - **Two distinct user-facing error messages**, per build-plan.md: "Could not extract text from this PDF. Please try a different file." when `pdf-parse` returns under 50 chars of text (PDF-quality problem), vs. "Could not extract profile details from this resume. Please fill in the fields manually." when all three AI providers fail after text was successfully read (AI problem).
  - **Verified end-to-end by the developer** in their own authenticated browser session (no browser-automation tool was available to this agent — same constraint as `06`) after the bugs below were found live and fixed. Extraction now successfully populates `ProfileForm` from a real resume.
  - **Bug fixed — `pdf-parse` v2's pdfjs-dist worker couldn't resolve inside Turbopack's server bundle** (`Setting up fake worker failed: Cannot find module '...pdf.worker.mjs'`). Tried the narrower fix first (`PDFParse.setWorker(getPath())` from `pdf-parse/worker` inside the route) — that import eagerly pulls in `@napi-rs/canvas`'s `CanvasFactory`, and Turbopack can't statically resolve that package's per-platform native binary (`@napi-rs/canvas-win32-x64-msvc`), so the narrower fix just traded one bundler error for another. Fixed properly at the right layer: added `serverExternalPackages: ["pdf-parse", "@napi-rs/canvas"]` to `next.config.ts`, which tells Next.js to let Node `require()` these two packages directly at runtime instead of statically bundling them — this is `pdf-parse`'s own documented Next.js integration pattern. **Requires a full dev server restart to take effect** — config file changes don't hot-reload.
  - **Bug fixed — OpenAI rejected `max_tokens`** (`400 Unsupported parameter: 'max_tokens' is not supported with this model. Use 'max_completion_tokens' instead`) — `gpt-5.4-mini` requires the newer field name; Gemini/Groq's compat layers still expect the legacy one. `ProviderConfig` in `lib/ai-client.ts` now has a `tokensParam: "max_tokens" | "max_completion_tokens"` field set per provider, and the request body is built with `[provider.tokensParam]: maxTokens` instead of a hardcoded field name.
  - **Bug fixed — Groq's `openai/gpt-oss-20b` returned empty `content`** — it's a reasoning model and was spending the token budget on hidden reasoning before the visible JSON answer. Fixed two ways: added `extraBody: { reasoning_effort: "low" }` for the Groq provider config (extraction doesn't need deep reasoning), and raised `EXTRACTION_MAX_TOKENS` in `agent/extractor.ts` from 800 → 2000 (the original 800 was sized for non-reasoning `gpt-4o`, which no longer applies to any of the three current models). `callProvider` now builds its request as `Record<string, unknown>` and casts to the SDK's param type (`as unknown as ChatCompletionCreateParamsNonStreaming`) to allow `extraBody`'s provider-specific fields through — commented inline per code-standards.md's rule on necessary type assertions.
  - **Bug fixed — Gemini returned unparseable JSON** (`SyntaxError: Unterminated string`) — model emitted raw newlines inside string values instead of escaping them. Added `repairUnescapedControlChars()` in `lib/ai-client.ts` (tracks in-string state, re-escapes `\n`/`\r`/`\t` found inside strings) as a retry path when the first `JSON.parse` fails, and strengthened the extraction prompt to explicitly ask for escaped line breaks.
  - **Provider order changed mid-session**: built Gemini → Groq → OpenAI first per the original plan, then reordered to **OpenAI → Gemini → Groq** once live testing showed the developer's OpenAI key is paid/active while the Groq key was initially invalid and an earlier OpenAI key had been over quota — OpenAI now goes first as the confirmed-reliable provider.
  - **`GROQ_API_KEY` and `OPENAI_API_KEY` billing/validity were account-side issues fixed by the developer directly** (invalid key, exceeded quota) — not code bugs, no code changed for these two.
- **08 Resume PDF Generation from Profile:** Planned via `/architect`. Key decisions:
  - **AI scope is prose-only.** `agent/resume-generator.ts`'s `generateResumeContent()` calls `callWithFallback()` (the same 07 fallback chain — OpenAI → Gemini → Groq, no feature-08-specific provider changes) to write a 2-3 sentence professional summary and up to 4 bullet points per work experience role. Every factual field (name, contact info, dates, company names, school, skill tags) renders straight from the `profiles` row, untouched by the AI — never let the model alter facts, only rephrase responsibilities into bullets. If a provider returns a mismatched bullet-group count despite the prompt's explicit instruction, falls back to the role's original `responsibilities` text as a single bullet rather than dropping the role.
  - **No `agent_run`/`agent_logs` row for this flow** — same precedent as 07's extraction (no run concept exists for either flow in build-plan.md). Failures use the API Route Handler console.error pattern (`[api/resume/generate]`) instead.
  - **Single-page enforced by prompt constraints, not layout logic.** No dynamic font-shrinking or content-measurement was built — `agent/resume-generator.ts`'s system prompt caps bullet count (4/role) and summary length (2-3 sentences), and `agent/resume-pdf.tsx` uses a deliberately tight, dense Letter-size layout. This is a soft guarantee, not a hard one; revisit if real resumes with long content overflow a page.
  - **New shared `lib/profile-mapper.ts`** — extracted the `ProfileRow`/`mapRowToProfile()` pair that previously lived inline in `app/profile/page.tsx` so the new `/api/resume/generate` route (which also needs to turn a DB row into a `Profile`) doesn't duplicate it. `app/profile/page.tsx` now imports from here instead.
  - **`renderToBuffer`'s TypeScript signature requires the literal `<Document>` element**, not a wrapper component element — `agent/resume-pdf.tsx`'s `ResumeDocument` is called as a plain function (`ResumeDocument({ profile, content })`) in the route rather than via JSX/`createElement`, so the call expression's inferred type is the `<Document>` it returns. This also let the route file stay `route.ts` (code-standards.md's required naming) instead of needing `.tsx` for JSX support. Documented in `library-docs.md`.
  - **`insforge.storage` upload needs a `Blob`, not a raw `Buffer`** — and a Node `Buffer`'s `.buffer` property is typed `ArrayBufferLike` (covers `SharedArrayBuffer`), which isn't assignable to `BlobPart`. Fixed by wrapping in a fresh `Uint8Array(buffer)` before constructing the `Blob`, avoiding a type assertion.
  - **Same upsert + private-bucket-signed-URL pattern as 06's `uploadResume()`** — `bucket.remove(path)` then `bucket.upload(path, blob)` (no `upsert` flag exists), `resume_pdf_url` stores the storage path not a URL, and a fresh `createSignedUrl()` is minted and returned in the route's JSON response.
  - **Generated resume overwrites whatever was at `resumes/{user_id}/resume.pdf`** — uploaded original or a previous AI-generated version — there is only ever one active resume slot per architecture.md, by design.
  - **`ResumeUpload.tsx`'s "Generate Resume from Profile" button** (previously inert since 05) now calls `/api/resume/generate` directly via `fetch`, mirroring 07's `handleExtract` pattern. Since this is a Route Handler (not a Server Action), it doesn't get the automatic page refresh `uploadResume()`'s Server Action gets after `revalidatePath` — so the component tracks the freshly returned signed URL in local state (`generatedUrl`) and falls back to the server-rendered `resumePdfUrl` prop otherwise. `handleFile` (the upload path) clears this override so a subsequent manual upload isn't masked by a stale generated link.
  - **`@react-pdf/renderer` (4.5.1) installed clean** — no Turbopack `serverExternalPackages` workaround needed, unlike `pdf-parse`'s worker/canvas issue in 07.
  - **`library-docs.md` updated**: rewrote the stale `@react-pdf/renderer` section (old example used `<ResumePDF />` JSX + `contentType`/`upsert` options that don't exist) and the InsForge `### Storage` section (old `getPublicUrl()`/`contentType`/`upsert` example, which 06 had already silently deviated from) to match what's actually implemented. **`architecture.md` and `code-standards.md` still say "gpt-4o" project-wide** — that broader rename is still deferred, scoped out of this feature same as last session.
  - Not yet verified in a real authenticated browser session (no browser-automation tool available to this agent) — `tsc --noEmit`, `npm run lint`, and `npm run build` all pass clean. Ask the developer to generate a resume from their own profile and confirm the PDF looks right before calling this fully done.
  - **`/review` follow-up — added a download affordance.** Caught via real usage: there was no way to get the actual file, only "View current resume" (opens inline in a new tab). New `app/api/resume/download/route.ts` (GET) re-downloads the file server-side via `insforge.storage.from("resumes").download(path)` (the same SDK method `extract/route.ts` already uses) and returns it with `Content-Disposition: attachment` set itself — deliberately not just adding a `download` attribute to the existing signed-URL link, since `download` is ignored by browsers on cross-origin links (the signed URL points at InsForge's storage domain, not this app's origin) unless the remote response sets that header, which isn't under this app's control. **Documented exception to code-standards.md's `{ success, data?, error? }` JSON envelope rule for API routes** — this route returns a raw PDF body so a plain `<a href>` can trigger a real browser download; error paths before the file is read still return the standard JSON envelope.

---

- **09 Find Jobs Page — Full UI:** Planned via `/architect` before building, since the design (`context/designs/find-jobs.png`) disagreed with the project docs in three places. Confirmed with the developer:
  - **Match score bar colors follow the design's actual pixels**, not either conflicting doc — ui-rules.md says 80-100/60-79/<60 → green/blue/orange, ui-tokens.md says 90-100 & 70-89/50-69/<50 → green/orange/gray, but the mockup's own sample scores (94/96/91% green, 88/85% blue, 72% orange) match neither table. Built `scoreColorClass()` in `JobsTable.tsx` as `>=90` green, `80-89` blue, `<80` orange. **ui-rules.md and ui-tokens.md should be reconciled into one shared table** before feature 12 (Job Details) needs the same bar again — see `ui-registry.md`'s `JobsTable` entry.
  - **SOURCE column (Search/URL badge) added despite the mockup showing only 5 columns** (no Source) — the developer chose to follow build-plan.md's explicit column list over matching the design exactly here. No existing ui-tokens.md mapping for `'search'` (only `LinkedIn`/`URL` are defined) — chose `bg-info-lightest`/`text-info-foreground` for `'search'`, all mock rows use it since Adzuna is the only source feature 10 will build.
  - **"Jobs by Adzuna" credit (required by project-overview.md "on all job listings") deliberately omitted** — doesn't appear anywhere in the mockup, and the developer chose design-exact-match here (the opposite call from the Source-column decision above). Flagged in case a later design surfaces where this credit actually belongs.
  - **New components**, all Server Components (no `"use client"`, no state — pure mock UI, no logic per build-plan.md's "Full UI first" principle): `components/find-jobs/{SearchControls,JobFilters,JobsTable,JobsPagination}.tsx`, assembled in a rewritten `app/find-jobs/page.tsx`. New pill-shaped `<select>` pattern introduced in `JobFilters` (`rounded-full` + absolutely-positioned `ChevronDown`) — distinct from `ProfileForm`'s rectangular form-field `inputClass`, since these are compact list filters, not form fields. See `ui-registry.md` for full class breakdowns.
  - **Added `Job`/`JobSource` types to `types/index.ts`**, field names matching architecture.md's `jobs` table columns (`title`, `salary`, `source`, `foundAt`) rather than the UI's column labels (`ROLE`, etc.) — minimizes rework when feature 10/11 swap the 6 hardcoded mock rows (Vercel/Stripe/Linear/Notion/OpenAI/Figma, copied verbatim from the mockup) for real DB data.
  - **Deleted the `ComingSoon` placeholder usage on `/find-jobs`** per its own documented instruction in `ui-registry.md`. The component itself is untouched — `/dashboard` still uses it until Phase 5.
  - **Not verified in a real authenticated browser session** — `tsc --noEmit`, `npm run lint`, and `npm run build` all pass clean; confirmed via `curl` that `/find-jobs` 307-redirects to `/login` under the auth proxy with no server error. No OAuth credentials available to this agent to actually view the authenticated page (same constraint as 06/07/08) — developer chose to proceed without browser verification rather than checking it themselves first; ask them to confirm the rendered page matches `find-jobs.png` before fully trusting this is pixel-correct.

- **10 Adzuna Job Discovery:** Planned via `/architect` before building; checked features 11-17 to confirm pagination/filter/sort wiring is explicitly feature 11's scope (left `JobsPagination` static, per that decision). Key decisions:
  - **Found and fixed a real bug in `.env.local`**: `ADZUNA_APP_KEY` was typo'd as `ADZUNA_APP_LEY`, which would have made every Adzuna call silently fail with an unauthenticated/undefined key. Fixed (one-character rename, same value).
  - **Batched AI scoring, not per-job** — `agent/adzuna.ts`'s `scoreJobs()` sends all jobs returned by one Adzuna search to `callWithFallback()` in a single call (one prompt with the job array in, one schema-validated array of scores out, matched back to jobs by index) instead of one fallback-chain call per job. Cuts up to 10 sequential 3-provider-fallback calls down to 1 per search.
  - **Scoring failure is non-fatal** — if Adzuna itself fails, the whole run fails (`agent_runs.status = 'failed'`, no jobs saved, error returned to the UI). If Adzuna succeeds but the batched AI call fails entirely (all 3 providers, or a response whose `scores` array length doesn't match the job count), the jobs are still saved with `match_score: 0` and `match_reason: "Scoring is temporarily unavailable for this job."` rather than discarding real search results — the scoring failure itself is logged via the new `logAgentError()`.
  - **New shared `lib/agent-logger.ts`** (`logAgentError(runId, userId, message, jobId?)`) — a small `agent_logs` insert, added now (not feature-10-specific) since feature 13 (Company Research) will need the exact same logging and `code-standards.md` mandates every agent function log failures there.
  - **New `lib/job-mapper.ts`** (`JobRow` + `mapRowToJob()`) mirrors the existing `lib/profile-mapper.ts` precedent exactly. **`types/index.ts`'s `Job` type expanded** from feature 09's 7 display-only fields to the full `jobs` table shape (`location`, `jobType`, `aboutRole`, `matchReason`, `matchedSkills`, `missingSkills`, `sourceUrl`, `externalApplyUrl`, `runId`) — feature 12 (Job Details) needs these same fields, so this avoids a second expansion later.
  - **`MATCH_THRESHOLD` added to `lib/utils.ts`** — `code-standards.md` mandated this constant since feature 07, but no prior feature actually needed a match threshold until now. Set to `70` per that doc.
  - **Country detection implemented, not deferred** — `lib/adzuna.ts`'s `detectCountry()` is a small keyword map (uk/united kingdom/england/scotland/wales/london→`gb`, canada/toronto/vancouver/montreal→`ca`, australia/sydney/melbourne/brisbane→`au`, else `us`). Not real geocoding, but fixes the real bug of every search defaulting to `'us'` regardless of what the user typed in Location (which would silently return wrong/empty results for e.g. "London" or "Toronto" search).
  - **Append, not replace** — each search's jobs are inserted as new `jobs` rows tagged with the new `agent_runs.id` as `run_id`; previous searches' rows are untouched. Matches the `run_id` relationship in architecture.md.
  - **`SearchControls.tsx` is now `"use client"`** (see `ui-registry.md`) — owns the search inputs, calls `/api/agent/find`, and calls `router.refresh()` on success so `app/find-jobs/page.tsx` (a Server Component) re-fetches the real job list. New error-banner pattern added (`bg-error`/`text-error-foreground`, solid) since ui-tokens.md has no light/lightest error variant to pair with the existing light success banner.
  - **`app/find-jobs/page.tsx`** now an async Server Component: auth-redirects to `/login` if no user (mirrors `/profile`'s pattern), fetches all of the current user's `jobs` rows ordered by `found_at desc`, maps via `mapRowToJob`. `MOCK_JOBS` removed entirely.
  - **`JobsTable.tsx`** gets a new empty state (`jobs.length === 0` → single message row) since the table can now legitimately render zero real rows for a new user, and now formats `found_at` via a new `formatRelativeTime()` helper in `lib/utils.ts` instead of feature 09's hardcoded mock strings.
  - Not yet verified in a real authenticated browser session (same recurring constraint as 06-09) — `tsc --noEmit`, `npm run lint`, and `npm run build` all pass clean, and `/api/agent/find` + the updated `/find-jobs` route both appear correctly in the build's route list. Ask the developer to run a real search from their own session and confirm jobs appear, the success/error banner behaves, and Adzuna/AI keys are actually valid end-to-end.
  - **Bug fixed (caught via real usage, post-11): non-US location searches always returned "No jobs found."** Root cause: `detectCountry()`'s keyword map only covered `gb`/`ca`/`au` — every other location (including 8 of Adzuna's 12 actually-supported countries: `de`, `fr`, `nz`, `in`, `pl`, `br`, `at`, `za`) silently fell back to `'us'`, so e.g. searching "Berlin" queried Adzuna's **US** endpoint with `where=Berlin`, which correctly returns 0 results — the location text was being used, just against the wrong country's index. Fixed by expanding the keyword map to all 12 Adzuna-supported countries (verified against Adzuna's current docs via web search, not assumed) and having `detectCountry()` return `{ country, recognized, isRemote }` instead of a bare string. When `recognized` is `false` (location given but matches none of the 12), the API now returns an honest message naming the 12 supported regions instead of the generic "try a different title or location" — `library-docs.md`'s Adzuna rules section updated to list all 12 codes so this doesn't regress. Also added a `"remote"/"anywhere"/"wfh"` special case: these aren't real places Adzuna can geocode, so `where` is omitted entirely instead of being sent literally (which guaranteed 0 results).
  - **Feature added (same fix, per explicit developer request): blank job title / location now default to the user's profile** instead of blocking the search. `app/api/agent/find/route.ts` resolves `jobTitle` as "whatever the user typed, else `profile.jobTitlesSeeking`/`profile.currentTitle`" — input always wins when present, profile only fills genuinely empty fields. The old hard requirement ("Job title is required", `SearchControls`' button disabled when empty) is gone; the 422 now only fires if both the input *and* the profile have nothing usable.
  - **Follow-up iteration (superseded the free-text location field from the bullet above): location is now a closed country dropdown, not free text.** Developer's reasoning: with only 12 countries actually supported, a text field that can silently miss is worse than a dropdown that always works. Confirmed via web search against Adzuna's own docs (their `where` example is a literal place name like `"london"`, with no documented "remote"/"anywhere" support and no dedicated remote-job filter) — per the developer's explicit instruction, **"Remote" was deliberately NOT added** as a dropdown option since it isn't real Adzuna functionality. New `ADZUNA_COUNTRIES` constant in `lib/utils.ts` (the 12 `{ code, label }` pairs) is the single source of truth for both `SearchControls`' `<select>` and the route's server-side validation (`isSupportedCountryCode()` in `lib/adzuna.ts`) — eliminates the entire class of "guessed the wrong country" bugs for direct user input, since the dropdown can only ever submit a code that's actually valid. `detectCountry()`'s keyword-matching is now used for exactly one remaining case: mapping `profile.preferredLocations` free text to a country when the user leaves the dropdown on "Use my profile." `searchJobs()` dropped the `where` parameter entirely — country-path-only filtering, no city/region granularity (a deliberate scope reduction in exchange for reliability). `agent/adzuna.ts`'s `discoverJobs()` simplified to take a pre-resolved `country` code instead of doing its own detection internally.
  - **Scope change (developer-decided, caught via real usage): category widened from `it-jobs` only to `it-jobs` + `creative-design-jobs`.** Searching "uiux" in France returned a single, unrelated "Senior Software Engineer" posting — root cause was project-overview.md's original "category filtered to IT jobs" decision: genuine UI/UX/Product/Graphic Designer postings are filed under Adzuna's separate "Creative & Design Jobs" category, so they were excluded before the `what=uiux` keyword search ever ran; the one IT-job result that did appear only matched because it happened to mention "UI/UX" in its description text (Adzuna's `what` is full-text, not title-only). Presented the developer three options (widen category, keep IT-only and warn instead, or drop the category filter entirely) — chose to widen. `lib/adzuna.ts`'s `searchJobs()` now queries `ADZUNA_CATEGORIES` (`["it-jobs", "creative-design-jobs"]`) in parallel via `Promise.all` and merges/dedupes by job `id` — Adzuna's `category` param only accepts one tag per request, there's no multi-category syntax, so two real requests are made, not one. `results_per_page` dropped from `10` to `5` per category to keep the combined total (and AI-scoring batch size/token budget in `agent/adzuna.ts`) unchanged from before. `project-overview.md`'s feature list line updated to match. `agent/adzuna.ts`'s `discoverJobs()` signature unchanged — it still just calls `searchJobs(jobTitle, country)`, the multi-category fan-out is fully internal to `lib/adzuna.ts`.
  - **Bug fixed (caught via real usage, post-dropdown): every search 500'd with Postgres error `23514` — "violates check constraint jobs_job_type_check".** Root cause: `agent/adzuna.ts` inserted `job.contract_type` (Adzuna's raw value, e.g. `"permanent"`) directly into `jobs.job_type`, but that column's CHECK constraint (from `04 Database Schema`) only allows `'fulltime' | 'parttime' | 'contract'` per architecture.md — `"permanent"` isn't one of the three, so the insert failed on every single job, every search. Fixed with a new `mapJobType()` in `agent/adzuna.ts`: `contract_type === "contract"` → `"contract"`, `contract_time === "part_time"` → `"parttime"`, else → `"fulltime"` (covers `contract_type: "permanent"` and the common case of both fields missing). Added the missing `contract_time` field to the `AdzunaJob` type in `lib/adzuna.ts` (only `contract_type` was typed before). `library-docs.md`'s "Saving Jobs to DB" example updated to call `mapJobType()` instead of showing `job.contract_type || "fulltime"`, since that exact line was the bug, copy-pasted into the docs as if it were correct.
  - **Feature added (developer request): Job Title input is now a Google-style autocomplete.** New `lib/job-titles.ts` (`SUPPORTED_JOB_TITLES` — curated, not exhaustive, spanning both `ADZUNA_CATEGORIES`; `filterJobTitles()` — prefix matches ranked above substring matches, capped at 8). This is an assist, not a whitelist — free text outside the list still searches Adzuna exactly as before, per the standing "let user search for any job" decision from earlier in this same feature's history. `SearchControls.tsx` rewritten with `role="combobox"`/`role="listbox"`/`role="option"` ARIA wiring, keyboard nav (ArrowUp/Down cycles `highlightedIndex` with wraparound, Enter selects the highlighted suggestion or falls through to a normal search if none is highlighted, Escape closes), and a 150ms blur-close delay (the standard fix for blur firing before a suggestion's click event registers). Clicking/selecting a suggestion both fills the field and **immediately runs the search** — `handleSearch()` gained an optional `titleOverride` param so `selectSuggestion()` doesn't have to rely on a stale `jobTitle` closure right after `setJobTitle()`. `ui-registry.md`'s `SearchControls` entry has the full breakdown.
  - **Bug fixed (caught via real usage): table appeared not to update after a successful search, even though `router.refresh()` was firing correctly and the jobs were genuinely saved.** Diagnosed by querying the DB directly (`run-raw-sql`): a "software engineer"/`us` search correctly saved 10 real, well-reasoned jobs (confirmed not a scoring-fallback artifact) — but this user already had 81 saved jobs total with only 8 scoring ≥70, and the new 10 scored 0-58. With `JobFilters`' default sort (Match Score, descending) and 20-results-per-page, those new low-scoring jobs were almost certainly sorted onto page 2+ by older higher-scoring jobs already in the DB — or hidden entirely if a "High Match" filter happened to be left active from earlier testing. `router.refresh()` was working exactly as designed; the *visible* page-1/current-filter view just didn't happen to include the new rows. Fixed in `SearchControls.tsx`: a successful search now does `router.push(\`${pathname}?sort=newest\`)` instead of a bare `router.refresh()` — this resets `q`/`match`/`page` to their defaults and forces sort to "Newest," so the just-found jobs (which all share a near-identical `found_at` from the same insert) are guaranteed to land at the very top of page 1 regardless of score or whatever the table was previously showing.

- **11 Filter + Sort + Pagination:** Planned via `/architect`. Key decisions:
  - **All filter/sort/search/page state lives in URL search params** (`?q=&match=&sort=&page=`), not client state — keeps `app/find-jobs/page.tsx` the single place doing data fetching (code-standards.md: "Data fetching happens in Server Components — never fetch in Client Components directly"), and makes filtered views bookmarkable/shareable/refresh-safe. New `lib/jobs-query.ts` (`parseJobsSearchParams()`, `escapeIlikeTerm()`, `JOBS_PAGE_SIZE = 20`) centralizes parsing/validation of the four params (invalid/missing values fall back to `all`/`score`/page `1`).
  - **Filtering, sorting, and pagination all happen at the DB layer**, not in JS — confirmed the installed `@insforge/sdk` wraps `@supabase/postgrest-js` directly (its own type defs reference `PostgrestFilterBuilder`), so the full filter API is really available: `.or('company.ilike."%term%",title.ilike."%term%"')` for the company-or-title text search, `.gte('match_score', MATCH_THRESHOLD)` / `.lt(...)` for High/Low Match (reusing the constant from feature 10), `.order()` for sort, `.range(offset, offset + 19)` + `select('*', { count: 'exact' })` for the 20-per-page slice and total count.
  - **Search term escaping, to prevent filter-string injection/breakage:** `escapeIlikeTerm()` backslash-escapes `%`/`_` (so a literal `%` or `_` typed by the user isn't treated as an `ilike` wildcard), and the term is wrapped in double quotes inside the `.or()` string (PostgREST's documented escape for commas/parentheses inside a filter value) so a search containing `,` or `()` can't be parsed as extra filter conditions.
  - **Only `JobFilters` became `"use client"`** (same precedent as `SearchControls` in feature 10) — `JobsPagination` stayed a Server Component; its Previous/page-number/Next controls are plain `next/link` `<Link>`s built from the page's own `searchParams`, no client JS needed for pagination at all.
  - **World-class UX details, explicitly requested over a bare-minimum implementation:** text search is debounced 350ms before updating the URL (avoids a server round-trip per keystroke); the two `<select>`s update immediately (discrete choices, no debounce needed); everything is wrapped in `useTransition` so input never blocks, with the filter bar dimming to `opacity-60` while a navigation is pending instead of a jarring full-page loading state; any filter/search/sort change strips the `page` param so you're never left on, say, page 3 of a result set that's now only 1 page long; both `JobFilters`' own URL updates and every `JobsPagination` link use `scroll: false`/`scroll={false}` so paging or filtering never yanks the viewport back to the top of the page.
  - **`JobsTable`'s empty state split in two** via a new `hasActiveFilters` prop — "No jobs found yet — search above to get started." only when the user has zero jobs *and* no filters active; "No jobs match your filters — try adjusting your search or match filter." when filters/search are active but matched nothing. The feature-09/10 single empty-state message would otherwise tell a user with dozens of saved jobs to "get started" just because their filter combo matched zero rows.
  - **State-sync pattern in `JobFilters` avoids `useEffect`:** syncing the debounced text input's local state back to the URL's `q` value (e.g. on browser back/forward) is done via React's documented "adjust state during render" pattern (compare `searchParams.get("q")` against a tracked `syncedQuery` state each render and update both together if they differ) rather than a `useEffect` + `setState`, since the installed ESLint config's `react-hooks/set-state-in-effect` rule flags that as an error, not just a warning.
  - Not yet verified in a real authenticated browser session (same recurring constraint as every feature since 06) — `tsc --noEmit`, `npm run lint`, and `npm run build` all pass clean. Ask the developer to try each filter, both sorts, the text search, and paging through more than 20 real jobs to confirm the PostgREST query chain behaves as expected end-to-end.

- **12 Job Details Page — Full UI:** Built directly from `context/designs/job-details.png` (pixel-sampled via a cropped/zoomed inspection of the actual PNG, not eyeballed) since the developer explicitly asked for an exact match. Key decisions:
  - **Resolved the match-score color conflict flagged since feature 09/11 — but not the way expected.** ui-rules.md, ui-tokens.md, and `JobsTable`'s own `scoreColorClass()` each disagree on a three-tier bar threshold, and the open question carried in `memory.md` asked whether to extract `scoreColorClass()` into a shared helper for this page. Pixel-sampling the design's "85% Match Score" badge showed it isn't a three-tier bar at all — it's `#EBFDF5`/`#007A55`, an exact match for ui-tokens.md's already-defined **Status Badges** pairing (`bg-success-lightest`/`text-success-foreground` for "High Match"). The header badge is binary, gated on the existing `MATCH_THRESHOLD` (70) constant — `>= 70` → the High Match pairing, otherwise the Low Match pairing (`bg-surface-secondary`/`text-text-secondary`). **No extraction of `scoreColorClass()` happened** — it's a different concept (continuous three-tier bar fill vs. binary status pill) and the three-tier bar conflict in ui-rules.md/ui-tokens.md is still unreconciled for any future component that needs an actual bar. Flagging that doc conflict as still open, scoped narrower now that it's confirmed not to block this page.
  - **Skills badges needed no new decision** — pixel-sampled both the matched-skill chips (`#EBFDF5`/`#11B981`-family → `bg-success-lightest`/`text-success-foreground`) and the missing-skill chips (`#FAF5FF`/`#7C5DFC` → `bg-accent-muted`/`text-accent`) and both landed exactly on ui-tokens.md's pre-existing "Skills Badges" table — just sized larger (`px-3 py-1.5 text-sm`, not the smaller `px-2 py-0.5 text-xs` badge convention used for table/status badges) to match the design's actual chip size, with a leading `Check`/`X` icon.
  - **Design labels this section "You have" / "Gap skills"**, not build-plan.md's "matched skills" / "missing skills (red/orange)" wording — design took precedence (same precedent as every prior UI feature), and the underlying data/field names (`matchedSkills`/`missingSkills`) are unchanged, only the on-screen copy differs. **Missing skills render in accent/purple, not red/orange** — build-plan.md said red/orange, but the design and ui-tokens.md's existing Skills Badges table both already say accent; design and the existing token agree with each other, build-plan.md's prose is the outlier here.
  - **`Job Description` renders `job.aboutRole` as a single paragraph, not separate Responsibilities/Requirements/Benefits sections.** project-overview.md's prose describes a more elaborate structured breakdown, but feature 10 never actually built those fields — `about_role` stores Adzuna's raw description snippet as one string (confirmed in `lib/job-mapper.ts`/`library-docs.md`), and the design itself renders it as one continuous block, not bulleted subsections. Building separate sections would have required inventing data that doesn't exist anywhere in the pipeline. Not a regression — just confirms the simpler reality already shipped in 10.
  - **Company Research card is empty-state only, exactly per build-plan.md's scope line for this feature** — `components/job-details/CompanyResearch.tsx`'s "Research Company" button has no `onClick` (same "build the UI, wire it later" precedent as 05's inert Save Profile button before 06). Feature 13 wires the click handler; nothing here needs to change when it does, since the component already takes the real `job` for the empty-state copy (`Click "Research Company" to let the AI browse {company}'s public pages...`).
  - **Info-card icon colors were pixel-sampled, not guessed**, since ui-tokens.md has no existing mapping for "info card icon chip": Salary → `bg-success-lightest`/`text-success`, Location → `bg-info-lightest`/`text-info-medium`, Job Type → `bg-accent-muted`/`text-accent`, Date Found → `bg-surface-secondary`/`text-text-secondary`. All four resolved to existing tokens — no new token needed.
  - **`JobsTable` row navigation wired** (`components/find-jobs/JobsTable.tsx` is now `"use client"`, `onClick={() => router.push(\`/find-jobs/${job.id}\`)}` on each row) — feature 11's own comment explicitly deferred this exact thing ("cursor-pointer... navigation isn't wired yet — that route doesn't exist until Phase 4"). Now that Phase 4's route exists, wiring it is closing that documented loose end, not new scope. No new a11y semantics (`role`, keyboard handling) were added beyond the existing `cursor-pointer` — same minimal-interactivity level as the rest of the table.
  - **`app/find-jobs/[id]/page.tsx`** is an async Server Component, `params` awaited as a `Promise<{ id: string }>` per the installed Next.js 16 docs (confirmed in `node_modules/next/dist/docs`, not assumed from training data) — same async-params pattern as `searchParams` in `app/find-jobs/page.tsx`. Auth-redirects to `/login` if no user; queries `jobs` scoped to both `id` and `user_id` (never just `id` — code-standards.md's "always scope to user" rule applies even though `id` alone is already unique, since a user must never be able to view another user's job by guessing a UUID) and calls `notFound()` if the row doesn't exist or doesn't belong to them.
  - **Five new files in `components/job-details/`** matching architecture.md's planned folder list exactly: `JobInfo.tsx` (header card + 4 info cards), `MatchScore.tsx` (AI Match Reasoning + Required Skills vs Your Profile), `JobDescription.tsx`, `CompanyResearch.tsx`, `JobActions.tsx` (Apply Now button). `JobInfo.tsx` owns "View Job Post" (header-card action, links to `sourceUrl`) and `JobActions.tsx` owns "Apply Now" (footer action, links to `externalApplyUrl`) — both currently resolve to the same Adzuna `redirect_url` value per feature 10's mapping, but kept as separate fields/buttons matching their distinct names and design positions.
  - Verified with `tsc --noEmit`, `npm run lint`, and `npm run build` (route `/find-jobs/[id]` appears in the build output) — and confirmed against three real saved jobs in the developer's own account via direct SQL (`mcp__insforge__run-raw-sql`), not just mock data. **Not yet browser-verified** (same recurring constraint as every feature since 06) — ask the developer to open `/find-jobs` and click into a real job row, or visit `/find-jobs/921bfa1e-bb48-4435-9cb8-28b4cf95ec28` directly, to confirm the page matches `job-details.png`.

- **13 Company Research Agent:** Planned via `/architect`. Neither `@browserbasehq/sdk` nor `@browserbasehq/stagehand` were installed before this feature, despite `library-docs.md` having full code patterns for both — both installed fresh (`sdk@2.14.1`, `stagehand@3.6.0`). Key decisions:
  - **Stagehand v3 has a materially different API than `library-docs.md`'s documented v2-style pattern** — confirmed by reading the installed package's own `.d.ts` files before writing any code, per AGENTS.md's "this is not the library you know" warning. `extract({ instruction, schema })` (object form) is gone; v3 is positional: `stagehand.extract(instruction, zodSchema, options?)`. Page navigation is `stagehand.context.activePage()!.goto(url, { waitUntil })`, not a bare `activePage()` call with no navigation method shown in the old docs. The exported class name `Stagehand` is now an alias for an internal `V3` class. `library-docs.md` should be rewritten before any other feature touches Stagehand — not done in this session, scoped to this feature's actual diff only (same precedent as 07/08's stale-doc notes).
  - **No separate `lib/browserbase.ts` or manual `bb.sessions.create()` call** — v3's `V3Options.browserbaseSessionCreateParams` lets `new Stagehand({ env: "BROWSERBASE", apiKey, browserbaseSessionCreateParams: { projectId, timeout }, ... })` create and manage its own Browserbase session internally. The old docs' two-step "create session via SDK, then pass `browserbaseSessionID` to Stagehand" pattern is unnecessary in v3 and would have been a redundant abstraction.
  - **Stagehand's own extraction LLM uses `gpt-5.4-mini`, not `gpt-4o`** — `library-docs.md`'s pattern hardcoded `gpt-4o`, but that model was confirmed retired (Feb 17, 2026) during feature 07's session. Reused the same current OpenAI model the rest of the app already standardized on.
  - **Synthesis goes through the shared `callWithFallback()` chain** (`lib/ai-client.ts`, OpenAI → Gemini → Groq), not a raw `gpt-5.4-mini` call as build-plan.md's prose literally states — confirmed with the developer via `/architect`. This was explicitly built as shared infra back in feature 07 "so 08/10/13 can adopt the same fallback later"; feature 13 is that adoption. Temperature `0.4`, `maxTokens: 800` per `library-docs.md`'s existing table.
  - **`logAgentError`'s `runId` param widened from `string` to `string | null`** (`lib/agent-logger.ts`) — confirmed against the live schema (`agent_logs.run_id` is nullable) that this is the honest type. Company research has no `agent_runs` row of its own (it operates on one already-saved job, not a "run"), so it logs against the job's own `run_id`, which is theoretically `null` for any future URL-ingested job (architecture.md: "null if from URL input" — no such ingestion path exists yet, but the type no longer lies about it).
  - **Re-research is allowed, not one-shot** — confirmed with the developer. `jobs.company_research` can be overwritten any time via a "Re-research" action that appears once a dossier exists, replacing the original "Research Company" button.
  - **`deriveHomepageUrl()`** (`agent/company-research.ts`) follows the Adzuna `redirect_url` server-side via `fetch(url, { redirect: "follow" })`, reads `response.url`, and strips to the root domain (last two hostname labels) unless the redirect never left `adzuna.com` or the fetch throws — in which case it falls back to `https://www.{slugified company name}.com`, per build-plan.md's spec.
  - **Sub-page selection ranks non-careers links first**, capped at 3 total (`MAX_SUBPAGES`) — careers pages are sorted last rather than excluded, per build-plan.md's "prefer about/blog/engineering/product over careers" guidance, not a hard exclusion.
  - **Every Stagehand `extract()` call and the session close are individually try/caught** — a single sub-page failure logs via `logAgentError()` and continues to the next link rather than aborting the whole research; if the *homepage* extraction itself fails or returns empty (`oneLiner`/`productSummary` both blank), browsing is abandoned entirely and synthesis runs from the job description + profile alone, per build-plan.md's "never return empty" rule. The session is always closed in a `finally` block.
  - **New `CompanyDossier` type in `types/index.ts`**, `companyResearch: CompanyDossier | null` added to `Job`; `lib/job-mapper.ts`'s `mapRowToJob()`/`JobRow` updated to map `company_research`. No design mockup exists for the filled dossier UI (`job-details.png` only shows the empty state) — built from build-plan.md's 9-field list using `MatchScore.tsx`'s established card/badge conventions; new `bg-info-lightest`/`text-info-foreground` tag color introduced for Tech Stack specifically to avoid reusing the `success`/`accent` matched/missing-skill color pairing for an unrelated neutral-fact field. See `ui-registry.md`'s `CompanyResearch` entry for the full breakdown.
  - **`CompanyResearch.tsx` converted from a Server Component to `"use client"`** — same fetch-then-`router.refresh()` pattern as `SearchControls.tsx`, including its error-banner styling.
  - Verified with `tsc --noEmit`, `npm run lint`, and `npm run build` (route `/api/agent/research` appears in the build output) — all clean. **Not yet browser-verified or tested against a real Browserbase session** (same recurring constraint as every feature since 06 — no browser-automation credentials available to this agent for the authenticated app, and a live Browserbase run wasn't triggered this session). Ask the developer to click "Research Company" on a real saved job and confirm: the loading state shows, a dossier renders with real (not hallucinated) company facts, "Re-research" appears and works, and `agent_logs` stays empty on a clean run.
  - **Bug fixed (caught via real usage, first live test): every research run 502'd — all 3 AI providers failed synthesis** (`openai: Unterminated string in JSON`, `gemini: Request timed out`, `groq: didn't match the expected shape`). Root cause, found via `/review`: the synthesis `systemPrompt` said only "Return ONLY valid JSON" with **no explicit field shape** — unlike `agent/extractor.ts`'s established pattern of spelling out the exact JSON shape (field names + types) in the prompt. Without it, each provider guessed a different shape/verbosity, which also meant `maxTokens: 800` (copied from `library-docs.md`'s table) regularly truncated the response mid-string, and `dossierSchema`'s all-required fields failed validation outright on any response missing/nulling one field. Fixed three ways: (1) `SYSTEM_PROMPT` now spells out the literal 9-field JSON shape, same convention as `extractor.ts`; (2) `dossierSchema` uses the same `nullableString`/`nullableStringArray` defensive pattern as `extractor.ts`, with a new `normalizeDossier()` collapsing nulls to `""`/`[]` before the result is typed as `CompanyDossier`; (3) `SYNTHESIS_MAX_TOKENS` raised `800` → `2000`. Also added an optional `timeoutMs` to `lib/ai-client.ts`'s `CallWithFallbackParams` (default unchanged at the existing 20s for every other caller) and set `SYNTHESIS_TIMEOUT_MS = 35_000` for company research specifically, since its prompt (full homepage + up to 3 sub-page extractions) is meaningfully larger than the prompts that established the 20s figure. `library-docs.md`'s Stagehand section and max-tokens table updated to match.

- **14 Dashboard Page — Full UI:** Built pixel-for-pixel from `context/designs/dashboard.png` (cropped/sampled, same practice as 12/13) per the developer's explicit "build exactly as shown" instruction. Key decisions:
  - **New dependency installed: `recharts@3.9.0`.** Not on code-standards.md's original approved list, but build-plan.md's feature 17 explicitly names it for the three dashboard charts — installed now (not deferred) so the chart components built with mock data in this feature don't need rebuilding when 17 wires real PostHog data, only their data source changes. Added to code-standards.md's approved list and a new dedicated section in `library-docs.md` covering the project-specific patterns (SVG `fill`/`stroke` must be `var(--color-*)` strings, never Tailwind classes — Recharts colors via real SVG presentation attributes, not CSS classes; gradient fills via `<linearGradient>` in `<defs>`; axis tick color reuses `--color-text-muted` since ui-tokens.md's literal `#9CA3AF` axis-label spec has no backing token, same precedent as `AppNavbar`'s inactive-link color).
  - **`context7` MCP was unreachable (network error)** when checked for current Recharts docs before building — proceeded with the standard `ResponsiveContainer`/`AreaChart`/`BarChart` API (stable across recent major versions), confirmed against the installed package's own `.d.ts` exports rather than assumed from training data, then verified end-to-end via `tsc`/`lint`/`build` all passing clean.
  - **Stats, Recent Activity, and all three charts are 100% mock data** — `components/dashboard/{StatsBar,RecentActivity,AnalyticsCharts}.tsx` — matching build-plan.md's "Full UI built with mock data first" core principle exactly; values were read off the actual design's bar heights/curve shape, not invented. **The profile-incomplete banner is the one real exception** — `app/dashboard/page.tsx` fetches the real profile and conditionally renders the existing `CompletionIndicator` (reused as-is from `/profile`, no new component) when `!isComplete`, per project-overview.md's explicit "Dashboard shows incomplete profile banner if profile not finished." The design screenshot doesn't show this state since that account has a complete profile.
  - **Stat card #4 label follows the design/project-overview.md ("Jobs This Week"), not build-plan.md's stale "Cover Letters Generated"** — cover letter generation is explicitly out of scope (project-overview.md's Features Out of Scope list), so build-plan.md's feature 14 line is a leftover from an earlier/different scope draft. Design + project-overview.md's own stat list agree with each other; build-plan.md is the one outlier.
  - **Recent Activity dot colors are positional, not semantic, in this mock** — pixel-confirmed the design alternates accent/info/success across its 5 rows independent of whether the row is a "Found jobs" or "Researched company" entry (e.g. "Researched Stripe" is blue, "Researched Vercel" is purple). Reused ui-tokens.md's existing 3-color Activity Dot pairs as-is to match pixels exactly. **Flagged for feature 16:** build-plan.md's feature 16 spec only describes 2 real activity types with "info blue, success green" — feature 16 will need to decide how 2 real types map onto 3 mock colors (drop one color, or fix one color per type instead of alternating).
  - **`AnalyticsCharts.tsx` renders three sibling chart cards with no wrapping `<div>`**, matching architecture.md's single planned file for all three charts — `app/dashboard/page.tsx` puts `<RecentActivity />` then `<AnalyticsCharts />` inside one `grid grid-cols-1 lg:grid-cols-2 gap-6`, so with 4 total grid items in a 2-column grid, CSS auto-flow lands them in the design's exact layout (Recent Activity beside Company Research Activity, the other two charts on the row below) with no manual row/col-span needed.
  - **Deleted `components/layout/ComingSoon.tsx` entirely** (not just its usage) — `/dashboard` was the last route still using it (feature 09 already removed it from `/find-jobs`), and grep confirmed zero remaining references anywhere in the app after this feature. Matches the component's own documented deletion instruction from when it was introduced in Phase 2.
  - Verified with `tsc --noEmit`, `npm run lint`, and `npm run build` (route `/dashboard` appears, all clean) plus a real `curl` check that `/dashboard` 307-redirects to `/login` under the auth proxy. **Not browser-verified by a human** (same recurring constraint as every feature since 06 — no auth credentials available to this agent). Ask the developer to log in and confirm `/dashboard` matches `dashboard.png` pixel-for-pixel, especially the two chart types (bar vs. gradient area) and the 2-column auto-flow layout at different viewport widths.

- **AppNavbar — added Sign Out + signed-in user display (developer request, not a numbered build-plan feature).** Closes a gap flagged repeatedly since feature 02/03: `signOutAction` (`actions/auth.ts`) existed since Phase 1 but had no UI calling it. Placed in the shared `AppNavbar` (global, all 4 logged-in pages) rather than scoped to just `/profile`, per the developer's explicit choice — reachable from `/dashboard` and `/find-jobs` too, not just after navigating to Profile first. New required `userEmail: string` prop on `AppNavbar`, passed as `user.email ?? ""` from `app/{profile,dashboard,find-jobs,find-jobs/[id]}/page.tsx` — all four already fetch `user` for their own auth redirect, so no new DB query was needed. Dropdown UI follows `SearchControls`' existing suggestion-dropdown shadow/border/blur-close pattern; Sign Out reuses `signOutAction` via the same form+Server-Action pattern as `OAuthButton`. See `ui-registry.md`'s `AppNavbar` entry for the full breakdown. Verified with `tsc --noEmit`, `npm run lint`, `npm run build` — all clean. **Not browser-verified** (same recurring constraint as every feature since 06).

- **15 Stats Bar — Real Data:** `StatsBar.tsx` converted from owning hardcoded mock data to a pure presentational component — takes a `stats: DashboardStat[]` prop and renders it, no logic of its own. New `lib/dashboard-stats.ts` (`computeDashboardStats()`) computes all four build-plan-specified values from one query result: `app/dashboard/page.tsx` fetches `match_score, found_at, company_research` for every job belonging to the current user (a single `Promise.all`'d query alongside the existing profile fetch, only the three columns actually needed — not `select('*')`) and the four stats (total count, average match score, company-research count, jobs-in-last-7-days count) are all derived client-side in this one helper from that one row set, rather than guessing whether the installed PostgREST version supports `avg()`/aggregate `select` syntax.
  - **Trend pills (the "+12%"/"+3%" badges on the first two cards in feature 14's mock) are now real week-over-week comparisons, not dropped** — build-plan.md's feature 15 spec only lists the four base values with no trend formula, but the design (carried from feature 14) visibly shows trend pills on Total Jobs Found/Avg. Match Rate, and the comparison is fully derivable from data already being fetched (`found_at` for a 7-day-window count comparison, `match_score` for a point-difference average comparison) — no new column or query needed. Chose to compute real trends over silently deleting a piece of the established design.
  - **Trend hides itself (`undefined`, no pill rendered) whenever there's no real 8–14-day-old comparison window** — verified directly against both real accounts via `run-raw-sql`: every job either account has ever found was inserted within the last 7 days (no jobs in the prior week), so both trends correctly render with no pill rather than a division-by-zero or a fabricated "+100%". This is the expected/correct behavior for any account whose job history doesn't yet span two full weeks, not a bug.
  - **No new "down trend" color was introduced** — ui-tokens.md's Trend Badge spec only defines one pairing (`bg-success-lightest`/`text-success-darker`), with no documented variant for a negative change. A real negative week-over-week trend (e.g. fewer jobs found this week than last) will still render in the same green pairing, just with a `-` sign in the text — flagged here rather than inventing an unbacked red/down token, same precedent as every other "doc doesn't cover this case" decision in this file.
  - Verified with `tsc --noEmit`, `npm run lint`, `npm run build` (all clean) plus the live-data cross-check above via direct SQL against both real accounts (91 jobs / 31% avg / 0 researched / 91 this week for `starlordflash@gmail.com`; 60 / 35% / 2 / 60 for the second account) — confirms the computed values match the database exactly, not just that the code compiles. **Not browser-verified** (same recurring constraint as every feature since 06 — no auth credentials available to this agent). Ask the developer to load `/dashboard` and confirm the four cards show these real numbers instead of the old mock 284/82%/35/28.

- **16 Recent Activity — Real Data:** `RecentActivity.tsx` converted to presentational (`activities: ActivityItem[]` prop, new empty state for brand-new users with zero activity). New `lib/recent-activity.ts` (`buildRecentActivity()`) merges two real sources fetched in `app/dashboard/page.tsx`: `agent_runs` rows where `status = 'completed'` (→ "Found X jobs for {title}", `success`/green dot) and `jobs` rows where `company_research IS NOT NULL` (→ "Researched {company}", `info`/blue dot), sorted by each entry's own timestamp descending, sliced to the most recent 5 (matching feature 14's pixel-matched 5-row design, within build-plan's stated 5-10 range).
  - **Resolves the color-mapping question feature 14 flagged**: build-plan.md's literal "info blue, success green" wording for the two real activity types, combined with ui-tokens.md's Activity Dots table already mapping "Job found" → `success`, leaves `info` as the only consistent choice for "Researched" — the mock's third `accent`/purple dot color is dropped entirely from real data, since only two real activity types exist.
  - **Known limitation, by necessity not oversight**: `jobs` has no dedicated "research performed at" column (architecture.md's schema only has `found_at`), so "Researched {company}" entries sort by `found_at` (when the job was discovered), not by when `company_research` was actually populated. The `company_researched` PostHog event does carry an accurate research timestamp, but pulling it in here would blur this feature's DB-only scope into 17's PostHog-only scope — kept the two data sources cleanly separated per the existing phase split. Verified against real data this isn't currently misleading (both real `starlordflash2@gmail.com` research entries share a `found_at` within ~1 second of their own discovery), but flagging in case a user researches an old job long after it was found, where this proxy would misplace it in the feed.
  - Verified against both real accounts via `run-raw-sql`: `starlordflash@gmail.com` has 14 completed runs and 0 researched jobs (confirms a real account can produce an activity feed made entirely of one entry type, not a bug); `starlordflash2@gmail.com` has both types interleaving correctly at the top of the sorted, sliced list. `tsc --noEmit`, `npm run lint`, `npm run build` all clean. **Not browser-verified** (same recurring constraint as every feature since 06).

- **17 Analytics Charts — Pivoted from PostHog Data to direct DB queries (developer-directed, same session).** First built against PostHog's Events REST API (`lib/posthog-query.ts`), then the developer pointed out a better approach and asked for a pivot: the installed `posthog-node` v5 SDK doesn't expose `.query()`, the Query API needs a personal API key the public project token can't provide, and — more fundamentally — the `jobs` table is the authoritative source the `job_found`/`company_researched` PostHog events were themselves derived from, so querying it directly is more reliable and cheaper than round-tripping through PostHog at all. **`lib/posthog-query.ts` deleted entirely**; `lib/dashboard-analytics.ts`'s `computeDashboardAnalytics()` is now a plain synchronous function over `StatsJobRow[]` (the exact same `match_score, found_at, company_research` row set feature 15 already fetches for `StatsBar` — `app/dashboard/page.tsx` now fetches `jobs` **once** and feeds it to both `computeDashboardStats()` and `computeDashboardAnalytics()`, removing what would have been a second query). `AnalyticsCharts.tsx`/`AnalyticsData`'s shape (`ChartPoint[]`/`MatchScoreBucket[]`) is unchanged, so the component itself didn't need to change at all — only its data source.
  - **The developer did add `POSTHOG_API_KEY`/`POSTHOG_PROJECT_ID` to `.env.local`** while this was being decided — removed the corresponding rows from `code-standards.md`'s env var table since no code references them anymore after the pivot (the names also don't match what `lib/posthog-query.ts` had been reading — `POSTHOG_PERSONAL_API_KEY` vs. the actual `POSTHOG_API_KEY` added — moot now, but flagging in case PostHog querying is revisited later and these credentials are reused).
  - **Match Score Distribution and Jobs Found Over Time are now strictly more accurate than the PostHog version would have been** — `match_score` is read straight from its real `integer` column (no `Number(event.properties.matchScore)` coercion needed), and `found_at` can't go missing the way a dropped/unflushed analytics event could.
  - **Company Research Activity still carries the same proxy-timestamp limitation already documented for feature 16's Recent Activity** — `jobs` has no "researched at" column, only `found_at`, so this chart buckets by when the job was *found*, not when research was actually run. This was the one place PostHog's `company_researched` event timestamp would have been more accurate than the DB, but mixing one PostHog-sourced chart with two DB-sourced ones would have reintroduced the exact dependency the pivot was meant to remove — accepted the same limitation already in place for the Recent Activity card instead.
  - Verified with `tsc --noEmit`, `npm run lint`, `npm run build` — all clean. Re-confirmed the match-score bucketing against live SQL on `starlordflash@gmail.com`'s real 91 jobs: 8/3/2/5/1 across the five buckets (50-60% through 90-100%), 72 below 50% correctly excluded — matches `buildMatchScoreDistribution()` exactly. **Not browser-verified** (same recurring constraint as every feature since 06) — ask the developer to load `/dashboard` and confirm all three charts now render real bars/lines instead of the empty-state message.

## Notes

- **03 PostHog Initialization:** `lib/posthog-client.ts`, `lib/posthog-server.ts`, and `app/providers.tsx` (init + manual `$pageview`) already existed from earlier work. Added the missing piece from library-docs.md's PostHog rules: `posthog.identify(userId)` after login / `posthog.reset()` on logout. None of the four approved events (`job_search_started`, `job_found`, `profile_completed`, `company_researched`) have a real trigger yet — Profile, Find Jobs, and Company Research don't exist until later phases — so no event capture calls were added; adding them now would mean firing them from nowhere or inventing new events, both against code-standards.md.
- **03 PostHog Initialization:** Sign-in/sign-out are server-driven (Server Action → redirect → route handler), so there's no single client-side "login"/"logout" moment to hook. Instead, `PostHogIdentify` in `app/providers.tsx` checks `insforge.auth.getCurrentUser()` on every pathname change and diffs against the last-identified user id (stored under `POSTHOG_LAST_IDENTIFIED_USER_KEY` in `localStorage`, see `lib/utils.ts`) to call `identify`/`reset` exactly once per transition. `signOutAction` in `actions/auth.ts` still has no UI calling it (no sign-out control exists in Navbar yet) — `reset()` is wired and will fire correctly once that control is added, but is currently untestable end-to-end for that reason.
- **06 Profile Save Logic — found and fixed a pre-existing data gap.** Both real accounts (`starlordflash@gmail.com`, `starlordflash2@gmail.com`) signed up during the `02 Auth` session, before `04 Database Schema`'s `handle_new_user()` trigger existed — neither had a `profiles` row, which 500'd `/profile` the moment real data fetching landed (`Cannot read properties of null (reading 'id')` in `mapRowToProfile`). Fixed with a one-time backfill INSERT (developer-approved) matching exactly what the trigger does for new signups — both accounts now have a row. This was a data gap, not a code bug: `app/profile/page.tsx` intentionally does not defensively handle a missing profile row, per the `04` decision that every authenticated user is guaranteed one. If this resurfaces for a new account, check whether the trigger itself is firing before adding code-level null handling.
- **06 Profile Save Logic — browser-verified by the developer.** TypeScript (`tsc --noEmit`) and `npm run lint` both passed cleanly pre-verification. The developer then hit the page directly in their own authenticated session (no browser-automation tool was available to this agent) and surfaced the data-gap bug above in real usage.

_Add notes here as the build progresses — workarounds, patterns, anything that differs from the context files._
